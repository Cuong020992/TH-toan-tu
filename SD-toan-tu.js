let input_height = prompt("nhập giá trị chiều cao: ");
let input_width = prompt("nhập giá trị chiều rộng: ");

let height = parseInt(input_height);
let width = parseInt(input_width);

let area = height * width;

document.write("Diện tích hình chữ nhật là: "+area);